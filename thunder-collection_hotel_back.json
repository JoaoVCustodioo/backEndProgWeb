{
    "/api/funcionarios/{funcionarioId}": {
      "method": "GET",
      "response_model": "schemas.Funcionario",
      "description": "Endpoint para obter um funcionário pelo ID",
      "parameters": {
        "funcionarioId": "ID do funcionário (inteiro)"
      }
    },
    "/api/funcionarios": {
      "method": "GET",
      "response_model": "schemas.PaginatedFuncionario",
      "description": "Endpoint para obter todos os funcionários",
      "parameters": {
        "offset": "Deslocamento (inteiro)",
        "limit": "Limite (inteiro)"
      }
    },
    "/api/funcionarios/signIn": {
      "method": "POST",
      "response_model": "schemas.Funcionario",
      "description": "Endpoint para criar um novo funcionário",
      "request_body": "schemas.funcionarioCreate"
    },
    "/api/funcionarios/{funcionarioId}/update": {
      "method": "PUT",
      "response_model": "schemas.Funcionario",
      "description": "Endpoint para atualizar um funcionário pelo ID",
      "parameters": {
        "funcionarioId": "ID do funcionário (inteiro)"
      },
      "request_body": "schemas.funcionarioCreate"
    },
    "/api/funcionarios{funcionarioId}/delete": {
      "method": "DELETE",
      "description": "Endpoint para excluir um funcionário pelo ID",
      "parameters": {
        "funcionarioId": "ID do funcionário (inteiro)"
      }
    }
  }